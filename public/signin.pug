doctype html
html(lang="en")
    head
        title="Sign In to ETN"
        style
            include styles/signin.css
    body
        main
            h1 Sign in to Evanston Township Notepad
            if fail
                p.fail Incorrect username or password
            form(method="post")
                input(type="text" name="username" autocomplete="off" placeholder="Username" required)
                input(type="password" name="password" placeholder="Password" required)
                input(type="submit" value="Sign in")
            br
            p
                a(href="/signup") I don't have an account yet!