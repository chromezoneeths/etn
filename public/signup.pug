doctype html
html(lang="en")
    head
        title="Sign Up for ETN"
        style
            include styles/signin.css
    body
        main.center
            h1 Sign up for Evanston Township Notepad
            if fail
                p.fail An error occured. Double check your entries
            form(method="post")
                input(type="text" name="username" autocomplete="off" placeholder="Username" required)
                input(type="password" name="password" placeholder="Password" required)
                input(type="password" name="password2" placeholder="Confirm Password" required)
                input(type="submit" value="Sign up")
            p Username must be at least 4 characters long and Password must be at least 8 characters long
            p Do not forget your password! Once it's gone, you can't get it back.
            br
            p
                a(href="/signin") I already have an account!